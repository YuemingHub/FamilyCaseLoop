# FamilyCaseLoop · 同行版（家庭教育个案全生命周期引导工具）

> 本仓库是「月明家庭教育系统」的**同行版**载体。
> 自用版见 `parent-reply-mvp`（月明本地调试沙盒，不在本仓库）。

## 一、定位

- **受众**：教育工作者 / 咨询师 / 研究者 / 家庭教育从业者（类似角色）。
- **形态**：一个个案从**开始到服务结束**的整个链路（S0–S8）的**引导前端**。
- **不重新发明逻辑**：直接复用本地工作台 `FamilyEduPortable` 的现有资产（工作流文档 / 方法论 / 知识库），以及自用版已代码化的引擎（安全门 / 双维诊断 / 七层透镜 / 路由 / 回复生成 / FM 拦截）。

## 二、与自用版的关系（防重复造）

| 版本 | 是什么 | 谁来验收 |
|---|---|---|
| **自用版** `parent-reply-mvp` | 引擎调试沙盒。月明自己不断跑、不断调，调到"觉得可以用了" | 月明本人 |
| **同行版** `FamilyCaseLoop`（本仓库） | 同一套引擎 + 工作流文档 + 知识库，包成**同行可自己走通**的引导界面 | 同行试用 + 月明复核 |

- 两边**共用 S3/S5/S6/S7 引擎**，不在同行版重写。
- 同行版 = 引导壳 + 资产编排层；逻辑内核在自用版 / 本地工作台。

## 三、个案全生命周期（S0–S8）↔ 本地资产

逐阶段对照见 [`docs/case-lifecycle-mapping.md`](docs/case-lifecycle-mapping.md)。
该映射已对照 `FamilyEduPortable` 工作台现有资产逐阶段核对，确认**链路本身工作台里早已存在**，同行版只是把它包成引导前端。

## 四、当前两个缺口（是"补容器"，不是"造逻辑"）

1. **真实案例容器为空**：`FamilyEduPortable/familyedu-os/active/private-library/09-client-service-records/active/` 当前不存在/为空（START.md 说的"真实在服务家庭"未落进去）。S8 的"服务档案"落点待建。
2. **诊断词表过窄**：双维诊断的判定词表过窄（"动太多/动太少"被误判成"未就绪"），S5 引擎需月明校准信号词才准。

## 五、安全底线（沿用 START.md 红线）

- **不面向家长、不部署外部机器人**。
- 未成年人高危场景（橙/红）严格隔离，必须人工复核，不接原始个案进对外链路。
- 密钥不出本机、不进版本库（见 `.gitignore`）。
- 内容转化（S9，若含）属"自媒体/对外"，按 `31_真实服务闭环主收口规则.md` 第 1 节"不适用"，需隔离、不接原始个案。

## 六、本仓库当前状态

- ✅ 仓库已建（GitHub: `YuemingHub/FamilyCaseLoop`）。
- ✅ 映射文档 + README 备好、本地提交成 git 仓库（ready 状态）。
- ⛔ **推送受阻**：当前运行环境出不了 `github.com:443`（DNS 可解析、TLS 被重置），故未能 push。待网络通或用户侧推送。
